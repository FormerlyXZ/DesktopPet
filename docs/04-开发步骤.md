# 开发步骤

## 阶段 1-8：基础桌宠（已完成）

<details>
<summary>已完成阶段详情（点击展开）</summary>

### 阶段 1：项目初始化 ✓
- 创建文件夹结构、编写文档、解压帧动画

### 阶段 2：基础桌宠窗口 ✓
- 透明无边框置顶窗口、加载第一帧、右下角定位

### 阶段 3：帧动画系统 ✓
- 帧序列加载器、QTimer播放器、悬停触发挥手动画

### 阶段 4：拖拽与点击交互 ✓
- 拖拽移动、点击弹出气泡面板

### 阶段 5：气泡面板与设置窗口 ✓
- 面板UI、设置窗口UI、淡蓝色主题

### 阶段 6：完善与打包 ✓
- 配置持久化、开机自启、PyInstaller打包

### 阶段 7：UI美化与交互优化 ✓
- 亚克力磨砂玻璃、动画选择下拉框、待机循环播放

### 阶段 8：面板过渡动画 ✓
- 滑入淡入/滑出淡出动画、默认待机动画修正

</details>

---

## 阶段 9：抽象接口 + PNG适配器（Q版-基础）

### 任务
- [ ] 创建 `src/character_base.py`：CharacterController 抽象基类
- [ ] 创建 `src/png_character.py`：包装 AnimationEngine，不改动原有代码

### 验证
- 运行程序，默认服装所有交互行为不变
- PngCharacter 正确代理所有 AnimationEngine 功能

---

## 阶段 10：GIF 注册表（Q版-素材发现）

### 任务
- [ ] 创建 `src/gif_registry.py`：扫描 `素材库/高木同学Q版gif/`
- [ ] 正则解析文件名提取动作名（takagi_{动作名}_{时间戳}.gif）
- [ ] 在 main.py 中临时测试，打印全部识别结果

### 验证
- 36个GIF全部正确识别
- list_actions() 输出中文动作名列表
- get_path(动作名) 返回正确文件路径

---

## 阶段 11：GIF 基础播放（Q版-渲染核心）

### 任务
- [ ] 创建 `src/gif_character.py` 骨架
- [ ] QMovie 加载 GIF + setMovie() 到 QLabel
- [ ] frameChanged → setMask() 更新透明区域
- [ ] 实现启动序列：到达 → 加载 → 加油
- [ ] 实现 start()/stop()/get_current_pixmap()

### 验证
- Q版窗口显示 GIF，透明区域正确
- 启动序列按序播放，最后停在加油循环
- PNG角色和GIF角色可独立运行

---

## 阶段 12：PetWindow 集成（双角色共存）

### 任务
- [ ] 重构 `pet_window.py`：self.character 替代 self.anim
- [ ] 实现双击检测（400ms QTimer.singleShot 延迟确认单击）
- [ ] 路由鼠标/面板事件到 CharacterController
- [ ] 更新 `main.py`：根据 config["character_type"] 创建对应角色
- [ ] 更新 `config.py`：添加 character_type 字段

### 验证
- 默认服装一切正常（回归测试）
- 修改 config 切换角色，Q版可运行
- Q版单击/双击区分正确

---

## 阶段 13：交互状态机（Q版-鼠标交互）

### 任务
- [ ] HOVER：鼠标进入→害羞、悬停3秒→问号、离开→待机
- [ ] CLICK：单击→惊吓、双击→生气→哭（链式，第二步不可打断）
- [ ] PANEL：面板按钮→点头、设置关闭/不保存→摇头
- [ ] 启动序列期间交互事件排队

### 验证
- 悬停/长悬停/单击/双击各反应正确
- 双击生气→哭链式播放
- 面板操作点头/摇头正确
- 启动序列中操作不被打断

---

## 阶段 14：行为检测（Q版-外设感知）

### 任务
- [ ] 创建 `src/input_monitor.py`：pynput QThread 监听
  - key_pressed 信号：任意键按下
  - any_activity 信号：键盘+鼠标活动
  - 优雅降级：pynput 失败时静默禁用
- [ ] 创建 `src/audio_monitor.py`：ctypes 音频检测
  - 500ms 轮询 IAudioMeterInformation.GetPeakValue()
  - 检测 IAudioEndpointVolume.GetMute()
  - 边沿触发，仅状态变化时发信号
- [ ] main.py 连线信号到 GifCharacter

### 验证
- 按任意键→打字GIF播放
- 播放音乐→唱歌GIF播放
- 系统静音→静音GIF播放
- 恢复声音/取消静音→无重复触发

---

## 阶段 15：AFK 随机系统（Q版-空闲娱乐）

### 任务
- [ ] 实现 AFK 30秒空闲计时器（进入IDLE时启动）
- [ ] 实现随机间隔计时器（1-3分钟随机）
- [ ] 从可用池中随机选择GIF播放
- [ ] 任何键盘/鼠标活动重置AFK计时

### 验证
- 30秒无操作→开始随机播放
- 随机间隔在1-3分钟范围内
- 动鼠标/按键后AFK停止，回到待机
- 再次空闲30秒后重新开始

---

## 阶段 16：设置面板重构（Q版-完整设置）

### 任务
- [ ] 创建 `src/gif_settings_page.py`：Q版专用设置页
  - 启动序列编辑器（QListWidget + 增删移按钮）
  - 行为检测映射（3个下拉框）
  - 鼠标交互映射（7个下拉框）
  - AFK设置（超时秒数、间隔范围、动画勾选池）
  - load_settings() / get_settings() 方法
- [ ] 重构 `src/settings_window.py`：
  - 添加角色选择下拉框
  - QStackedWidget 双页面（PNG页 / GIF页）
  - 窗口扩至 380×550
  - 新增信号：character_type_changed, gif_settings_changed
- [ ] 扩展 `src/config.py`：
  - 新增 DEFAULTS：gif_startup_sequence, gif_behavior_map, gif_mouse_map, gif_afk
  - deep_merge 递归合并（旧config平滑升级）
- [ ] main.py + pet_window.py 连线设置信号

### 验证
- 角色切换下拉框联动页面切换
- Q版各项设置可编辑：序列增删改序、映射修改、AFK勾选
- 保存后重启设置全部保留
- 旧 config.json 升级不丢失数据

---

## 阶段 17：打磨与收尾

### 任务
- [ ] pynput 初始化失败优雅降级
- [ ] GIF 加载失败跳过（不崩溃）
- [ ] 角色切换时清理旧状态（stop旧角色→start新角色）
- [ ] 回归测试默认服装所有交互
- [ ] 更新 devlog 最终记录

### 验证
- 所有验证清单通过
- 默认服装无回归
- 异常情况不崩溃
